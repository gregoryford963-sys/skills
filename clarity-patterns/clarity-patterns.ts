#!/usr/bin/env bun
/**
 * clarity-patterns.ts — Clarity smart contract pattern library
 * Patterns bundled from whoabuddy/claude-knowledge at build time.
 *
 * Usage: bun run clarity-patterns/clarity-patterns.ts <subcommand> [options]
 */

import { Command } from 'commander';

interface Pattern {
  name: string;
  slug: string;
  content: string;
}

interface PatternGroups {
  code: Pattern[];
  testing: Pattern[];
  registry: Pattern[];
}

const PATTERNS: PatternGroups = {"code": [{"name": "Public Function Template", "slug": "public-function-template", "content": "## Public Function Template\n\nStandard structure for public functions with guards and error handling:\n\n```clarity\n(define-public (transfer (amount uint) (to principal))\n  (begin\n    (asserts! (is-eq tx-sender owner) ERR_UNAUTHORIZED)\n    (try! (ft-transfer? TOKEN amount tx-sender to))\n    (ok true)))\n```\n\n- Use `try!` for subcalls to propagate errors\n- Use `asserts!` for guards before state changes\n- Add post-conditions on tx for asset safety"}, {"name": "Standardized Events", "slug": "standardized-events", "content": "## Standardized Events\n\nEmit structured events for off-chain indexing:\n\n```clarity\n(print {\n  notification: \"contract-event\",\n  payload: {\n    amount: amount,\n    sender: tx-sender,\n    recipient: to\n  }\n})\n```\n\n- `notification`: string identifier for the event type\n- `payload`: tuple with camelCase keys\n- Examples: [usabtc-token](https://github.com/USA-BTC/smart-contracts/blob/main/contracts/usabtc-token.clar), [ccd002-treasury-v3](https://github.com/citycoins/protocol/blob/main/contracts/extensions/ccd002-treasury-v3.clar)"}, {"name": "Error Handling with Match", "slug": "error-handling-with-match", "content": "## Error Handling with Match\n\nHandle external call failures gracefully:\n\n```clarity\n(match (contract-call? .other fn args)\n  success (ok success)\n  error (err ERR_EXTERNAL_CALL_FAILED))\n```"}, {"name": "Bit Flags for Status/Permissions", "slug": "bit-flags-for-status-permissions", "content": "## Bit Flags for Status/Permissions\n\nPack multiple booleans into a single uint:\n\n```clarity\n(define-constant STATUS_ACTIVE (pow u2 u0))   ;; 1\n(define-constant STATUS_PAID (pow u2 u1))     ;; 2\n(define-constant STATUS_VERIFIED (pow u2 u2)) ;; 4\n\n;; Pack multiple flags: (+ STATUS_ACTIVE STATUS_PAID) → u3\n;; Check flag: (> (bit-and status STATUS_ACTIVE) u0)\n;; Set flag: (var-set status (bit-or (var-get status) NEW_FLAG))\n;; Clear flag: (var-set status (bit-and (var-get status) (bit-not FLAG)))\n```\n\nExamples: [aibtc-action-proposal-voting](https://github.com/aibtcdev/aibtcdev-daos/blob/main/contracts/dao/extensions/aibtc-action-proposal-voting.clar)"}, {"name": "Multi-Send Pattern", "slug": "multi-send-pattern", "content": "## Multi-Send Pattern\n\nSend to multiple recipients in one transaction using fold:\n\n```clarity\n(define-private (send-maybe\n    (recipient {to: principal, ustx: uint})\n    (prior (response bool uint)))\n  (match prior\n    ok-result (let (\n      (to (get to recipient))\n      (ustx (get ustx recipient)))\n      (try! (stx-transfer? ustx tx-sender to))\n      (ok true))\n    err-result (err err-result)))\n\n(define-public (send-many (recipients (list 200 {to: principal, ustx: uint})))\n  (fold send-maybe recipients (ok true)))\n```"}, {"name": "Parent-Child Maps (Hierarchical Data)", "slug": "parent-child-maps-hierarchical-data", "content": "## Parent-Child Maps (Hierarchical Data)\n\nStore hierarchical data with pagination support:\n\n```clarity\n(define-map Parents uint {name: (string-ascii 32), lastChildId: uint})\n(define-map Children {parentId: uint, id: uint} uint)\n\n(define-read-only (get-child (parentId uint) (childId uint))\n  (map-get? Children {parentId: parentId, id: childId}))\n\n(define-private (is-some? (x (optional uint)))\n  (is-some x))\n\n(define-read-only (get-children (parentId uint) (shift uint))\n  (filter is-some?\n    (list\n      (get-child parentId (+ shift u1))\n      (get-child parentId (+ shift u2))\n      (get-child parentId (+ shift u3))\n      ;; ... up to page size\n    )))\n```"}, {"name": "Whitelisting (Assets/Contracts)", "slug": "whitelisting-assets-contracts", "content": "## Whitelisting (Assets/Contracts)\n\nControl which contracts/assets can interact:\n\n```clarity\n(define-map Allowed {contract: principal, type: uint} bool)\n\n;; Check in function\n(asserts! (default-to false (map-get? Allowed {contract: contract, type: type}))\n          ERR_NOT_ALLOWED)\n\n;; Batch update\n(define-public (set-allowed-list (items (list 100 {token: principal, enabled: bool})))\n  (ok (map set-iter items (ok true))))\n```\n\nExamples: [ccd002-treasury-v3](https://github.com/citycoins/protocol/blob/main/contracts/extensions/ccd002-treasury-v3.clar), [aibtc-agent-account](https://github.com/aibtcdev/aibtcdev-daos/blob/main/contracts/agent/aibtc-agent-account.clar)"}, {"name": "Trait Whitelisting", "slug": "trait-whitelisting", "content": "## Trait Whitelisting\n\nOnly allow calls from trusted trait implementations:\n\n```clarity\n(define-map TrustedTraits principal bool)\n\n;; In functions accepting traits\n(asserts! (default-to false (map-get? TrustedTraits (contract-of t)))\n          ERR_UNTRUSTED)\n```"}, {"name": "Delayed Activation", "slug": "delayed-activation", "content": "## Delayed Activation\n\nActivate functionality after a Bitcoin block delay:\n\n```clarity\n(define-constant DELAY u21000) ;; ~146 days in BTC blocks\n(define-data-var activation-block uint u0)\n\n;; Set on deploy or init\n(var-set activation-block (+ burn-block-height DELAY))\n\n(define-read-only (is-active?)\n  (>= burn-block-height (var-get activation-block)))\n```\n\nExample: [usabtc-token](https://github.com/USA-BTC/smart-contracts/blob/main/contracts/usabtc-token.clar)"}, {"name": "Rate Limiting", "slug": "rate-limiting", "content": "## Rate Limiting\n\nPrevent rapid repeated actions:\n\n```clarity\n(define-data-var last-action-block uint u0)\n\n(define-public (rate-limited-action)\n  (begin\n    (asserts! (> burn-block-height (var-get last-action-block)) ERR_RATE_LIMIT)\n    (var-set last-action-block burn-block-height)\n    ;; ... action\n    (ok true)))\n```"}, {"name": "DAO Proposals with Historic Balances", "slug": "dao-proposals-with-historic-balances", "content": "## DAO Proposals with Historic Balances\n\nUse `at-block` for snapshot voting:\n\n```clarity\n(define-map Proposals uint {\n  votesFor: uint,\n  votesAgainst: uint,\n  status: uint,\n  liquidTokens: uint,\n  blockHash: (buff 32)\n})\n\n;; Get voting power at proposal creation\n(define-read-only (get-vote-power (proposal-id uint) (voter principal))\n  (let ((proposal (unwrap! (map-get? Proposals proposal-id) u0)))\n    (at-block (get blockHash proposal)\n      (contract-call? .token get-balance voter))))\n\n;; Quorum check: (>= (/ (* total-votes u100) liquid-supply) QUORUM_PERCENT)\n```\n\nExample: [aibtc-action-proposal-voting](https://github.com/aibtcdev/aibtcdev-daos/blob/main/contracts/dao/extensions/aibtc-action-proposal-voting.clar)"}, {"name": "Fixed-Point Arithmetic", "slug": "fixed-point-arithmetic", "content": "## Fixed-Point Arithmetic\n\nHandle decimal values with scale factor:\n\n```clarity\n(define-constant SCALE (pow u10 u8)) ;; 8 decimal places\n\n;; Multiply then divide to preserve precision\n(define-read-only (calculate-share (amount uint) (percentage uint))\n  (/ (* amount percentage) SCALE))\n\n;; Convert to/from scaled values\n(define-read-only (to-scaled (amount uint))\n  (* amount SCALE))\n\n(define-read-only (from-scaled (amount uint))\n  (/ amount SCALE))\n```\n\nExample: [ccd012-redemption-nyc](https://github.com/citycoins/protocol/blob/main/contracts/extensions/ccd012-redemption-nyc.clar)"}, {"name": "Treasury Pattern with as-contract", "slug": "treasury-pattern-with-as-contract", "content": "## Treasury Pattern with as-contract\n\nUse `as-contract` for contract-controlled funds:\n\n```clarity\n(define-public (withdraw (amount uint) (recipient principal))\n  (begin\n    (asserts! (is-authorized tx-sender) ERR_UNAUTHORIZED)\n    (as-contract (stx-transfer? amount (as-contract tx-sender) recipient))))\n```\n\nWarning: `as-contract` changes both `tx-sender` and `contract-caller` to the contract principal.\n\n**tx-sender vs contract-caller decision framework:**\n- `tx-sender`: Use for auth checks, identity attribution, self-action guards. Preserves human identity through normal proxies (user -> proxy -> target). Preferred for composability.\n- `contract-caller`: Use when you need the IMMEDIATE caller identity specifically.\n- Security note: Using `contract-caller` for self-action guards (e.g., \"owner can't give themselves feedback\") is bypassable -- owner routes through any proxy and `contract-caller` shows the proxy, not the owner. `tx-sender` catches this because it preserves the human origin.\n\n| Call Path | contract-caller | tx-sender |\n|-----------|-----------------|-----------|\n| user -> target | user | user |\n| user -> proxy -> target | proxy | user |\n| user -> proxy (as-contract) -> target | proxy | proxy |\n\nExamples: [ccd002-treasury-v3](https://github.com/citycoins/protocol/blob/main/contracts/extensions/ccd002-treasury-v3.clar), [aibtc-agent-account](https://github.com/aibtcdev/aibtcdev-daos/blob/main/contracts/agent/aibtc-agent-account.clar)"}, {"name": "Clarity 4: Asset Restrictions", "slug": "clarity-4-asset-restrictions", "content": "## Clarity 4: Asset Restrictions\n\nRestrict what assets a contract call can move:\n\n```clarity\n(as-contract\n  (with-stx u1000000)                              ;; Allow 1 STX\n  (with-ft .token TOKEN u500)                      ;; Allow 500 fungible tokens\n  (with-nft .nft-contract NFT (list u1 u2 u3))    ;; Allow specific NFT IDs\n  ;; ... body\n)\n\n;; DANGER: Avoid unless necessary\n(with-all-assets-unsafe)\n```"}, {"name": "Multi-Party Coordination", "slug": "multi-party-coordination", "content": "## Multi-Party Coordination\n\nCoordinate actions requiring multiple signatures:\n\n```clarity\n;; Proposal state\n(define-map Intents uint {\n  participants: (list 20 principal),\n  accepts: uint,     ;; Bitmask of who accepted\n  status: uint,      ;; 0=pending, 1=ready, 2=executed, 3=cancelled\n  expiry: uint,\n  payload: (buff 256)\n})\n\n;; Accept via signature verification\n(define-public (accept (intent-id uint) (signature (buff 65)))\n  (let (\n    (intent (unwrap! (map-get? Intents intent-id) ERR_NOT_FOUND))\n    (msg-hash (sha256 (concat (int-to-ascii intent-id) (get payload intent))))\n    (signer (try! (secp256k1-recover? msg-hash signature))))\n    ;; Verify signer is participant, update accepts bitmask\n    (ok true)))\n```\n\nReference: ERC-8001 pattern for decidable multi-party coordination."}], "testing": [{"name": "Testing Pyramid", "slug": "testing-pyramid", "content": "## Testing Pyramid\n\n```\n┌─────────────────────────────────────┐\n│  Stxer (Historical Simulation)      │  Mainnet fork, pre-deployment validation\n├─────────────────────────────────────┤\n│  RV (Property-Based Fuzzing)        │  Invariants, edge cases, battle-grade\n├─────────────────────────────────────┤\n│  Vitest + Clarinet SDK              │  Integration tests, TypeScript\n├─────────────────────────────────────┤\n│  Clarunit                           │  Unit tests in Clarity itself\n└─────────────────────────────────────┘\n```"}, {"name": "When to Use Each Tool", "slug": "when-to-use-each-tool", "content": "## When to Use Each Tool\n\n| Tool | Use When | Skip When |\n|------|----------|-----------|\n| **Clarinet SDK** | Standard testing, CI/CD, type-safe | - |\n| **Clarunit** | Testing Clarity logic in Clarity, simple assertions | Complex multi-account flows |\n| **RV** | Treasuries, DAOs, high-value contracts, finding edge cases | Simple contracts, time pressure |\n| **Stxer** | Pre-mainnet validation, governance simulations | Early development, testnet-only |\n\n---"}, {"name": "Clarinet SDK (vitest)", "slug": "clarinet-sdk-vitest", "content": "## Clarinet SDK (vitest)\n\n### Setup\n\n```bash\nnpm install @hirosystems/clarinet-sdk vitest vitest-environment-clarinet\n```\n\n**vitest.config.js:**\n```javascript\nimport { defineConfig } from \"vitest/config\";\nimport { vitestSetupFilePath, getClarinetVitestsArgv } from \"@hirosystems/clarinet-sdk/vitest\";\n\nexport default defineConfig({\n  test: {\n    environment: \"clarinet\",\n    singleThread: true,\n    setupFiles: [vitestSetupFilePath],\n    environmentOptions: {\n      clarinet: getClarinetVitestsArgv(),\n    },\n  },\n});\n```\n\n### Key Gotchas\n\n- **NO `beforeAll`/`beforeEach`** - Simnet resets each test file session\n- **Single thread required** - `singleThread: true` for simnet isolation\n- **120s timeout for MXS** - Increase timeout when using mainnet fork\n\n### Test Structure (Arrange-Act-Assert)\n\n```typescript\nimport { Cl } from \"@stacks/transactions\";\nimport { describe, expect, it } from \"vitest\";\n\ndescribe(\"my-contract\", () => {\n  it(\"transfers tokens correctly\", () => {\n    // ARRANGE\n    const deployer = simnet.deployer;\n    const wallet1 = simnet.getAccounts().get(\"wallet_1\")!;\n    const amount = 100;\n\n    // ACT\n    const result = simnet.callPublicFn(\n      \"my-contract\",\n      \"transfer\",\n      [Cl.uint(amount), Cl.principal(wallet1)],\n      deployer\n    );\n\n    // ASSERT\n    expect(result.result).toBeOk(Cl.bool(true));\n  });\n});\n```\n\n### Clarity Value Conversions\n\n```typescript\nimport { Cl, cvToValue, cvToJSON } from \"@stacks/transactions\";\n\n// Building Clarity values\nCl.uint(100)                           // uint\nCl.int(-50)                            // int\nCl.bool(true)                          // bool\nCl.principal(\"SP123...\")               // principal\nCl.standardPrincipal(\"SP123...\")       // standard principal\nCl.contractPrincipal(\"SP123\", \"name\")  // contract principal\nCl.stringAscii(\"hello\")                // (string-ascii N)\nCl.stringUtf8(\"hello\")                 // (string-utf8 N)\nCl.bufferFromHex(\"deadbeef\")           // (buff N)\nCl.tuple({ amount: Cl.uint(100) })     // tuple\nCl.list([Cl.uint(1), Cl.uint(2)])      // list\nCl.some(Cl.uint(100))                  // (some value)\nCl.none()                              // none\n\n// Extracting JS values\ncvToValue(result.result)               // Generic conversion\ncvToValue(result.result, true)         // With bigint for uints\ncvToJSON(result.result)                // JSON-friendly format\n```\n\n### Custom Matchers\n\n```typescript\nexpect(result.result).toBeOk(Cl.uint(100));\nexpect(result.result).toBeErr(Cl.uint(1));\nexpect(result.result).toBeBool(true);\nexpect(result.result).toBeUint(100);\nexpect(result.result).toBePrincipal(\"SP123...\");\n```\n\n### Reusable Helpers Pattern\n\n```typescript\n// helpers/my-contract.ts\nexport function transfer(\n  simnet: Simnet,\n  amount: number,\n  recipient: string,\n  sender: string\n) {\n  return simnet.callPublicFn(\n    \"my-contract\",\n    \"transfer\",\n    [Cl.uint(amount), Cl.principal(recipient)],\n    sender\n  );\n}\n\nexport function getBalance(simnet: Simnet, owner: string) {\n  const result = simnet.callReadOnlyFn(\n    \"my-contract\",\n    \"get-balance\",\n    [Cl.principal(owner)],\n    owner\n  );\n  return cvToValue(result.result);\n}\n```\n\n### Type Generation Tools\n\n| Tool | Description |\n|------|-------------|\n| [clarigen](https://github.com/mechanismHQ/clarigen) | Generate TypeScript types from contracts |\n| [secondlayer](https://github.com/ryanwaits/secondlayer) | Alternative type generator |\n\n---"}, {"name": "Clarunit", "slug": "clarunit", "content": "## Clarunit\n\nTest Clarity with Clarity. Useful for pure logic testing.\n\n### Setup\n\n```bash\nnpm install @stacks/clarunit\n```\n\n**tests/clarunit.test.ts:**\n```typescript\nimport { clarunit } from \"@stacks/clarunit\";\nclarunit(simnet);\n```\n\n### Test File Convention\n\n- File: `tests/my-contract_test.clar`\n- Functions: Start with `test-`\n- Add to `Clarinet.toml` under `[contracts]`\n\n### Basic Test\n\n```clarity\n;; @name Multiplication works correctly\n(define-public (test-multiply)\n  (begin\n    (asserts! (is-eq u8 (contract-call? .math multiply u2 u4))\n      (err \"2 * 4 should equal 8\"))\n    (ok true)))\n```\n\n### Annotations\n\n| Annotation | Purpose |\n|------------|---------|\n| `@name` | Descriptive test name |\n| `@caller` | Override tx-sender (wallet name or `'SP...` principal) |\n| `@prepare` | Custom setup function |\n| `@no-prepare` | Skip default prepare |\n| `@mine-blocks-before N` | Mine N blocks before test |\n\n### Prepare Function\n\n```clarity\n(define-public (prepare)\n  (begin\n    (try! (contract-call? .my-contract init))\n    (ok true)))\n```\n\n### Flow Tests (Multi-Account)\n\nFile: `tests/my-contract_flow_test.clar`\n\n```clarity\n;; @name Vote and execute proposal\n;; @format-ignore\n(define-public (test-vote-flow)\n  (begin\n    ;; @caller wallet_1\n    (unwrap! (contract-call? .dao vote-yes) (err \"vote failed\"))\n    ;; @caller wallet_2\n    (unwrap! (contract-call? .dao vote-yes) (err \"vote failed\"))\n    ;; @caller deployer\n    (unwrap! (contract-call? .dao execute) (err \"execute failed\"))\n    (ok true)))\n```\n\n---"}, {"name": "RV (Rendezvous) - Fuzzy Testing", "slug": "rv-rendezvous-fuzzy-testing", "content": "## RV (Rendezvous) - Fuzzy Testing\n\nProperty-based testing for battle-grade contracts.\n\n### Setup\n\n```bash\nnpm install @stacks/rendezvous\n```\n\n### Test File Convention\n\n- File: `contracts/my-contract.tests.clar`\n- Properties: `test-*` (public functions)\n- Invariants: `invariant-*` (read-only functions)\n\n### Run Commands\n\n```bash\nnpx rv . my-contract test              # Property tests\nnpx rv . my-contract invariant         # Invariant tests\nnpx rv . my-contract test --runs=1000  # Custom iterations\nnpx rv . my-contract test --seed=12345 # Reproduce failure\n```\n\n### Property Test\n\n```clarity\n;; Property: loan amount always increases correctly\n(define-public (test-borrow (amount uint))\n  (if (is-eq amount u0)\n    (ok false)  ;; Discard invalid input\n    (let ((initial (get-loan tx-sender)))\n      (try! (borrow amount))\n      (asserts! (is-eq (get-loan tx-sender) (+ initial amount))\n        (err u999))\n      (ok true))))\n```\n\n### Invariant Test\n\n```clarity\n;; Invariant: total supply never exceeds cap\n(define-read-only (invariant-supply-capped)\n  (<= (var-get total-supply) MAX_SUPPLY))\n```\n\n### Return Values\n\n| Return | Meaning |\n|--------|---------|\n| `(ok true)` | Test passed |\n| `(ok false)` | Discard (invalid precondition) |\n| `(err ...)` | Test failed |\n\n---"}, {"name": "Stxer - Mainnet Simulations", "slug": "stxer-mainnet-simulations", "content": "## Stxer - Mainnet Simulations\n\nSimulate against real mainnet state before deployment.\n\n### Setup\n\n```bash\nnpm install stxer\n```\n\n### Basic Simulation\n\n```typescript\nimport { SimulationBuilder } from \"stxer\";\nimport { boolCV, uintCV } from \"@stacks/transactions\";\n\nSimulationBuilder.new()\n  .useBlockHeight(3491155)  // Fork mainnet at this block\n  .addContractDeploy({\n    contract_name: \"my-contract\",\n    source_code: contractSource,\n    deployer: \"SP123...\"\n  })\n  .addContractCall({\n    contract_id: \"SP123.my-contract\",\n    function_name: \"init\",\n    function_args: [boolCV(true)],\n    sender: \"SP123...\",\n    nonce: 1\n  })\n  .run()\n  .then(result => console.log(`View: https://stxer.xyz/simulations/${result.id}`));\n```\n\n### When to Use\n\n- Pre-mainnet deployment validation\n- Governance proposal testing\n- Testing with real token balances\n- Multi-transaction sequence validation\n\n---"}, {"name": "Project Structure (Full Stack)", "slug": "project-structure-full-stack", "content": "## Project Structure (Full Stack)\n\n```\nmy-project/\n├── Clarinet.toml\n├── vitest.config.js\n├── package.json\n├── contracts/\n│   ├── my-contract.clar\n│   └── my-contract.tests.clar      # RV tests\n├── tests/\n│   ├── my-contract.test.ts         # Vitest\n│   ├── my-contract_test.clar       # Clarunit\n│   └── clarunit.test.ts            # Clarunit runner\n└── simulations/\n    └── my-contract-stxer.ts        # Stxer\n```\n\n### package.json Scripts\n\n```json\n{\n  \"scripts\": {\n    \"test\": \"vitest run\",\n    \"test:watch\": \"vitest\",\n    \"test:rv\": \"npx rv . my-contract test\",\n    \"test:rv:invariant\": \"npx rv . my-contract invariant\",\n    \"test:stxer\": \"npx tsx simulations/my-contract-stxer.ts\"\n  }\n}\n```\n\n---"}, {"name": "Style Guide", "slug": "style-guide", "content": "## Style Guide\n\n### Prefer Functions Over Arrow Functions\n\n```typescript\n// Good\nfunction getBalance(owner: string) {\n  return simnet.callReadOnlyFn(...);\n}\n\n// Avoid\nconst getBalance = (owner: string) => {\n  return simnet.callReadOnlyFn(...);\n};\n```\n\n### Constants for Reusable Values\n\n```typescript\nconst DEPLOYER = simnet.deployer;\nconst WALLET_1 = simnet.getAccounts().get(\"wallet_1\")!;\nconst CONTRACT_NAME = \"my-contract\";\nconst ERR_UNAUTHORIZED = Cl.uint(1);\n```\n\n### Strong Typing\n\n```typescript\ninterface TransferResult {\n  success: boolean;\n  sender: string;\n  recipient: string;\n  amount: bigint;\n}\n\nfunction parseTransferResult(cv: ClarityValue): TransferResult {\n  // Type-safe parsing\n}\n```\n\n---"}, {"name": "References", "slug": "references", "content": "## References\n\n- Example repo: https://github.com/friedger/clarity-ccip-026\n- Clarinet docs: https://docs.hiro.so/clarinet\n- RV docs: https://stacks-network.github.io/rendezvous/\n- Stxer SDK: https://github.com/stxer/stxer-sdk\n- Clarigen: https://github.com/mechanismHQ/clarigen\n- Secondlayer: https://github.com/ryanwaits/secondlayer"}], "registry": [{"name": "Block Snapshot Pattern", "slug": "block-snapshot-pattern", "content": "## Block Snapshot Pattern\n\nCapture a comprehensive snapshot of chain state at transaction time. This is the \"receipt\" that makes a transaction worth the fee.\n\n### Full Snapshot Structure\n\n```clarity\n;; Maximum chain context at transaction time\n(define-read-only (get-block-snapshot)\n  {\n    ;; Block heights\n    stacksBlock: stacks-block-height,\n    burnBlock: burn-block-height,\n    tenure: tenure-height,\n\n    ;; Timing (Clarity 4)\n    blockTime: stacks-block-time,\n\n    ;; Chain identity\n    chainId: chain-id,\n\n    ;; Transaction context\n    txSender: tx-sender,\n    contractCaller: contract-caller,\n    txSponsor: tx-sponsor?,\n\n    ;; Block hashes (historical proof)\n    stacksBlockHash: (get-stacks-block-info? id-header-hash (- stacks-block-height u1)),\n    burnBlockHash: (get-burn-block-info? header-hash (- burn-block-height u1))\n  }\n)\n```\n\n### Snapshot Fields Explained\n\n| Field | Type | Description | Use Case |\n|-------|------|-------------|----------|\n| `stacksBlock` | `uint` | Current Stacks block height | Ordering, age calculation |\n| `burnBlock` | `uint` | Current Bitcoin block height | Bitcoin-anchored time |\n| `tenure` | `uint` | Miner tenure count | Nakamoto-aware timing |\n| `blockTime` | `uint` | Unix timestamp (Clarity 4) | Human-readable time |\n| `chainId` | `uint` | Network ID (1=mainnet) | Replay protection |\n| `txSender` | `principal` | Transaction originator | Attribution |\n| `contractCaller` | `principal` | Immediate caller | Proxy detection |\n| `txSponsor` | `(optional principal)` | Fee sponsor if any | Sponsored tx tracking |\n| `stacksBlockHash` | `(optional (buff 32))` | Previous Stacks block hash | Cryptographic anchor |\n| `burnBlockHash` | `(optional (buff 32))` | Previous Bitcoin block hash | BTC anchor |\n\n### Why Previous Block Hashes?\n\nWe capture `block-height - 1` because the current block's hash isn't finalized until after our transaction. The previous block's hash is:\n- Immutable and verifiable\n- Proves the transaction happened after that block\n- Can be independently verified against block explorers\n\n### Cost Considerations\n\n```clarity\n;; Minimal snapshot (cheapest)\n{stacksBlock: stacks-block-height, burnBlock: burn-block-height}\n\n;; Standard snapshot (balanced)\n{\n  stacksBlock: stacks-block-height,\n  burnBlock: burn-block-height,\n  blockTime: stacks-block-time,\n  txSender: tx-sender\n}\n\n;; Full snapshot (comprehensive - use for high-value records)\n;; Includes block hashes - adds ~2 read operations\n```\n\n### Helper Functions\n\n```clarity\n;; Get snapshot for storage (call at transaction time)\n(define-private (capture-snapshot)\n  {\n    stacksBlock: stacks-block-height,\n    burnBlock: burn-block-height,\n    tenure: tenure-height,\n    blockTime: stacks-block-time,\n    chainId: chain-id,\n    txSender: tx-sender,\n    contractCaller: contract-caller,\n    txSponsor: tx-sponsor?,\n    stacksBlockHash: (get-stacks-block-info? id-header-hash (- stacks-block-height u1)),\n    burnBlockHash: (get-burn-block-info? header-hash (- burn-block-height u1))\n  }\n)\n\n;; Type for storage\n(define-constant SNAPSHOT_TYPE\n  {\n    stacksBlock: uint,\n    burnBlock: uint,\n    tenure: uint,\n    blockTime: uint,\n    chainId: uint,\n    txSender: principal,\n    contractCaller: principal,\n    txSponsor: (optional principal),\n    stacksBlockHash: (optional (buff 32)),\n    burnBlockHash: (optional (buff 32))\n  }\n)\n```\n\n---"}, {"name": "Registry Key Patterns", "slug": "registry-key-patterns", "content": "## Registry Key Patterns\n\n### Principal-Keyed Registry\n\nUse when tracking state **per address** (heartbeats, profiles, balances):\n\n```clarity\n(define-map Registry\n  principal           ;; Key: who\n  {\n    ;; ... snapshot fields\n    count: uint       ;; Address-specific counter\n  }\n)\n\n;; Lookup\n(map-get? Registry address)\n\n;; Update (overwrites previous)\n(map-set Registry tx-sender {...})\n```\n\n**Characteristics:**\n- One entry per address\n- Overwrites on subsequent calls\n- Natural for \"last known state\" patterns\n\n### Hash-Keyed Registry\n\nUse when tracking **unique data** (attestations, commitments):\n\n```clarity\n(define-map Registry\n  (buff 32)           ;; Key: hash of data\n  {\n    ;; ... snapshot fields\n    attestor: principal\n  }\n)\n\n;; Lookup\n(map-get? Registry hash)\n\n;; Insert (first-write-wins)\n(asserts! (is-none (map-get? Registry hash)) ERR_ALREADY_EXISTS)\n(map-set Registry hash {...})\n```\n\n**Characteristics:**\n- One entry per unique hash\n- First attestor wins\n- Immutable once written\n\n### Composite-Keyed Registry\n\nUse for **multi-dimensional** tracking (votes per proposal, actions per agent):\n\n```clarity\n(define-map Registry\n  {entity: principal, action: uint}\n  {\n    ;; ... snapshot fields\n  }\n)\n\n;; Lookup\n(map-get? Registry {entity: address, action: action-id})\n```\n\n---"}, {"name": "Secondary Index Pattern", "slug": "secondary-index-pattern", "content": "## Secondary Index Pattern\n\nEnable enumeration of entries by address when primary key isn't the address.\n\n```clarity\n;; Primary: hash -> data\n(define-map Attestations (buff 32) {...})\n\n;; Secondary: address + index -> hash\n(define-map AttestorIndex\n  {attestor: principal, index: uint}\n  (buff 32)\n)\n\n;; Counter for next index\n(define-map AttestorCount principal uint)\n\n;; On insert:\n(let ((idx (default-to u0 (map-get? AttestorCount attestor))))\n  (map-set AttestorIndex {attestor: attestor, index: idx} hash)\n  (map-set AttestorCount attestor (+ idx u1)))\n\n;; Enumerate:\n(define-read-only (get-attestor-hash-at (attestor principal) (index uint))\n  (map-get? AttestorIndex {attestor: attestor, index: index}))\n```\n\n### Paginated Retrieval\n\n```clarity\n;; Get page of entries (client calls repeatedly with offset)\n(define-read-only (get-attestor-hashes (attestor principal) (offset uint))\n  (let ((count (default-to u0 (map-get? AttestorCount attestor))))\n    {\n      total: count,\n      items: (list\n        (get-attestor-hash-at attestor offset)\n        (get-attestor-hash-at attestor (+ offset u1))\n        (get-attestor-hash-at attestor (+ offset u2))\n        ;; ... up to page size\n      )\n    }\n  )\n)\n```\n\n---"}, {"name": "Global Stats Pattern", "slug": "global-stats-pattern", "content": "## Global Stats Pattern\n\nTrack aggregate metrics without iterating:\n\n```clarity\n(define-data-var totalEntries uint u0)\n(define-data-var uniqueAddresses uint u0)\n\n;; On new entry:\n(var-set totalEntries (+ (var-get totalEntries) u1))\n(if isNewAddress\n  (var-set uniqueAddresses (+ (var-get uniqueAddresses) u1))\n  true)\n\n;; Read stats:\n(define-read-only (get-stats)\n  {\n    totalEntries: (var-get totalEntries),\n    uniqueAddresses: (var-get uniqueAddresses)\n  }\n)\n```\n\n---"}, {"name": "Event Emission Pattern", "slug": "event-emission-pattern", "content": "## Event Emission Pattern\n\nStructured events for off-chain indexers:\n\n```clarity\n(print {\n  notification: \"registry-action\",  ;; Event type identifier\n  payload: {\n    ;; Include all relevant data for indexers\n    key: key,\n    actor: tx-sender,\n    action: \"create\",  ;; or \"update\", \"delete\"\n    ;; ... snapshot fields\n  }\n})\n```\n\n**Conventions:**\n- `notification`: String identifier for event routing\n- `payload`: Tuple with camelCase keys\n- Include enough data that indexers don't need to make follow-up calls\n\n---"}, {"name": "Write Semantics", "slug": "write-semantics", "content": "## Write Semantics\n\n### First-Write-Wins (Attestations)\n\n```clarity\n(define-constant ERR_ALREADY_EXISTS (err u1001))\n\n(define-public (attest (key (buff 32)))\n  (begin\n    (asserts! (is-none (map-get? Registry key)) ERR_ALREADY_EXISTS)\n    (map-set Registry key {...})\n    (ok true)))\n```\n\n### Last-Write-Wins (Heartbeats)\n\n```clarity\n(define-public (check-in)\n  (begin\n    ;; Always overwrites previous\n    (map-set Registry tx-sender {...})\n    (ok true)))\n```\n\n### Append-Only (History)\n\n```clarity\n(define-map History\n  {address: principal, index: uint}\n  {...snapshot...}\n)\n\n(define-public (record)\n  (let ((idx (default-to u0 (map-get? HistoryCount tx-sender))))\n    (map-set History {address: tx-sender, index: idx} {...})\n    (map-set HistoryCount tx-sender (+ idx u1))\n    (ok idx)))\n```\n\n---"}, {"name": "Access Control Patterns", "slug": "access-control-patterns", "content": "## Access Control Patterns\n\n### Open Registry (Anyone Can Write)\n\n```clarity\n(define-public (register)\n  (ok (map-set Registry tx-sender {...})))\n```\n\n### Self-Only Updates\n\n```clarity\n(define-public (update (data (buff 64)))\n  (begin\n    (asserts! (is-some (map-get? Registry tx-sender)) ERR_NOT_REGISTERED)\n    (map-set Registry tx-sender {...})\n    (ok true)))\n```\n\n### Admin-Gated Registration\n\n```clarity\n(define-data-var admin principal CONTRACT_OWNER)\n\n(define-public (register-address (address principal))\n  (begin\n    (asserts! (is-eq tx-sender (var-get admin)) ERR_UNAUTHORIZED)\n    (map-set Registry address {...})\n    (ok true)))\n```\n\n---"}, {"name": "Rate Limiting", "slug": "rate-limiting", "content": "## Rate Limiting\n\nPrevent spam by requiring minimum time between actions:\n\n```clarity\n(define-constant MIN_BLOCKS u10)\n(define-constant ERR_TOO_SOON (err u1002))\n\n(define-public (action)\n  (let ((existing (map-get? Registry tx-sender)))\n    (asserts!\n      (>= (- stacks-block-height (default-to u0 (get stacksBlock existing)))\n          MIN_BLOCKS)\n      ERR_TOO_SOON)\n    ;; ... proceed\n  ))\n```\n\n---"}, {"name": "Contract Template", "slug": "contract-template", "content": "## Contract Template\n\nMinimal registry contract combining these patterns:\n\n```clarity\n;; registry-template.clar\n\n;; Constants\n(define-constant CONTRACT_OWNER tx-sender)\n(define-constant ERR_UNAUTHORIZED (err u1000))\n(define-constant ERR_NOT_FOUND (err u1001))\n\n;; Data\n(define-map Registry\n  principal\n  {\n    stacksBlock: uint,\n    burnBlock: uint,\n    blockTime: uint,\n    chainId: uint,\n    count: uint\n  }\n)\n\n(define-data-var totalEntries uint u0)\n(define-data-var uniqueAddresses uint u0)\n\n;; Public\n(define-public (register)\n  (let\n    (\n      (existing (map-get? Registry tx-sender))\n      (isNew (is-none existing))\n      (newCount (+ (default-to u0 (get count existing)) u1))\n    )\n    (map-set Registry tx-sender {\n      stacksBlock: stacks-block-height,\n      burnBlock: burn-block-height,\n      blockTime: stacks-block-time,\n      chainId: chain-id,\n      count: newCount\n    })\n    (var-set totalEntries (+ (var-get totalEntries) u1))\n    (if isNew\n      (var-set uniqueAddresses (+ (var-get uniqueAddresses) u1))\n      true)\n    (print {notification: \"register\", payload: {address: tx-sender, count: newCount}})\n    (ok newCount)\n  )\n)\n\n;; Read-only\n(define-read-only (get-entry (address principal))\n  (map-get? Registry address))\n\n(define-read-only (get-stats)\n  {totalEntries: (var-get totalEntries), uniqueAddresses: (var-get uniqueAddresses)})\n```\n\n---"}, {"name": "Related", "slug": "related", "content": "## Related\n\n- `runbook/clarity-heartbeat-registry.md` - Principal-keyed with address enumeration\n- `runbook/clarity-proof-of-existence.md` - Hash-keyed with attestor enumeration\n- `patterns/clarity-patterns.md` - Additional patterns (events, rate limiting, etc.)\n- `context/clarity-reference.md` - Language reference"}]} as const;

const GROUPS = ["code", "testing", "registry"] as const;
type Group = typeof GROUPS[number];

function allPatterns(group?: string): Array<Pattern & { group: Group }> {
  const groups: Group[] = group && GROUPS.includes(group as Group) ? [group as Group] : [...GROUPS];
  return groups.flatMap(g => PATTERNS[g].map(p => ({ ...p, group: g })));
}

const program = new Command();
program.name("clarity-patterns").description("Clarity smart contract pattern library").version("1.0.0");

program.command("list")
  .description("List all available pattern categories")
  .option("--group <group>", "filter by group: code, testing, registry")
  .action((opts) => {
    const patterns = allPatterns(opts.group).map(p => ({ group: p.group, name: p.name, slug: p.slug }));
    console.log(JSON.stringify({ groups: GROUPS, total: patterns.length, patterns }, null, 2));
  });

program.command("get")
  .description("Get a pattern by slug or partial name")
  .option("--slug <slug>", "exact slug match")
  .option("--name <name>", "case-insensitive partial name match")
  .action((opts) => {
    if (!opts.slug && !opts.name) {
      console.log(JSON.stringify({ error: "Provide --slug or --name" }));
      process.exit(1);
    }
    const all = allPatterns();
    let match: (Pattern & { group: Group }) | undefined;
    if (opts.slug) {
      match = all.find(p => p.slug === opts.slug);
    } else {
      const term = opts.name.toLowerCase();
      match = all.find(p => p.name.toLowerCase().includes(term));
    }
    if (!match) {
      console.log(JSON.stringify({ error: "Pattern not found", tip: "Run \"list\" to see all available slugs" }));
      process.exit(1);
    }
    console.log(JSON.stringify({ group: match.group, name: match.name, slug: match.slug, content: match.content }, null, 2));
  });

program.command("search")
  .description("Search patterns by keyword")
  .requiredOption("--keyword <keyword>", "search term (case-insensitive)")
  .action((opts) => {
    const term = opts.keyword.toLowerCase();
    const matches = allPatterns()
      .filter(p => p.name.toLowerCase().includes(term) || p.content.toLowerCase().includes(term))
      .map(p => {
        const idx = p.content.toLowerCase().indexOf(term);
        const excerpt = idx >= 0 ? p.content.slice(Math.max(0, idx - 40), idx + 120).replace(/\n/g, " ") : "";
        return { group: p.group, name: p.name, slug: p.slug, excerpt };
      });
    console.log(JSON.stringify({ keyword: opts.keyword, count: matches.length, matches }, null, 2));
  });

program.command("all")
  .description("Output the full pattern library")
  .option("--group <group>", "filter by group: code, testing, registry")
  .action((opts) => {
    const groups: Record<string, Pattern[]> = {};
    const selected = opts.group && GROUPS.includes(opts.group as Group) ? [opts.group as Group] : [...GROUPS];
    for (const g of selected) { groups[g] = PATTERNS[g]; }
    console.log(JSON.stringify({
      version: "1.0.0",
      source: "whoabuddy/claude-knowledge",
      groups
    }, null, 2));
  });

program.parse();
